version: '3.8'

services:
  db:
    image: postgres:13-alpine
    environment:
      - POSTGRES_DB=app
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=q1w2e3r4t5y6
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data